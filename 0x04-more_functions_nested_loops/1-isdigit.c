#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks if character is digit
 * @c: character to be processed
 * Return: 1 if c is digitt else 0
 */
int _isdigit(int c)
{
	if
		(c >= 0 && c <= 9)
			return (1);
	else
			return (0);
}
